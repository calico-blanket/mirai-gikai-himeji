# Jevで分野分類の候補を作る

これは当サービスの整理であり、市の公式分類ではありません。2026年9月23日にユーザーが実APIで全52案件の応答を保存し、原文・出典・分類ルールとの対応を検証しました。分野候補を表示する案件は21件（うち複数分野9件）、分類保留は31件、未生成は0件です。52件とも人による分類確認前です。第140号は「子育て・教育」、第154号は「まちづくり・交通」「産業・観光」の候補です。第135号は判断材料の判定値0.69が試行基準0.8未満のため保留です。テスト用の応答を画面へ掲載していません。

## 今回の構成

- `lib/data/topic-classification.ts`：9分野の定義、Jevへの質問、入力・応答・保存候補の型と検証。型は既存のZodから導出します。
- `scripts/typesafe-client.mjs`：公式HTTP APIへの通信。新たなSDK依存は追加していません。
- `scripts/classify-himeji-topics.mjs`：資料検証、3件の試行、保存済み候補の再利用、全件生成・選択再生成。
- `scripts/run-topic-pilot.ps1`：Windows上でキーを非表示入力し、プロセス内だけに渡すための実行手順。`-All` を付けると未生成の残りも対象にします。
- `data/candidates/himeji-2025-4-topics.json`：実応答から作った候補だけを保存します。
- `lib/data/topics.ts`：ビルド時に現在の原文と候補を照合する読み込み口。APIは呼びません。
- `lib/data/topic-reviews.ts`：人の分類判断の型・原文と候補の版・履歴の検証。`data/candidates/himeji-2025-4-topic-reviews.json` に保存し、AI候補や公式原文とは分離します。
- `app/review/TopicReviewForm.tsx`：公式原文と候補を見比べ、1件ずつ確認・修正・保留を選んで確認記録をダウンロードする画面。
- `scripts/import-topic-reviews.mjs`：記録の事前検証と台帳への追記。既定は検証だけで、`--apply` 時だけ保存します。
- 議案一覧：分野・未作成・分類保留で絞り込み。議案詳細：分類と確認状態、入力原文・出典・取得日時。確認ページ：生成件数と原文照合フォーム。

## 分類に使う資料と判断

対象は令和7年第4回定例会です。公式HTMLの議案番号・名称・概要と、取得済み会議録の提案理由の説明を入力にします。提案説明の対応が未確認であること、複数議案を含む段落があることも入力と指示に含めます。議員の討論・賛否や現在の会派所属から分類しません。

9分野についてそれぞれ「該当するか」をNoulで問い、別に「判断材料が足りるか」を問います。複数分野を許容し、該当を強制しません。試行中の表示条件は判断材料の判定値が0.8以上、各分野の判定値が0.8以上です。この値は日本語議案で校正された基準ではなく、正解率を示しません。判定値が低い場合は保留、高い場合も `unreviewed` です。

モデルは公式資料で確認した `jev-1.13.0` に固定しています。モデル名、分類定義、質問の変更時は再評価が必要です。API仕様の参照先：

- [HTTP API](https://docs.typesafe.ai/api)
- [Noul](https://docs.typesafe.ai/primitives/noul)
- [判定値の扱い](https://docs.typesafe.ai/confidence)
- [モデルと日本語への注意](https://docs.typesafe.ai/models)

## キーなしで事前確認

プロジェクトのフォルダーで次を実行します。通信も候補ファイルの更新も行いません。

```powershell
npm run topics:prepare
```

既定の試行対象は議案第135号、第140号、第154号の3件です。入力サイズはバイト数で表示し、料金やトークン数とは混同しません。

## キーを保存せず3件を試す

キーをチャット、コマンドの文字列、JSON、`.env`等のファイルへ書かず、次のスクリプトの非表示入力を使ってください。

```powershell
.\scripts\run-topic-pilot.ps1
```

Windows標準のPowerShell 5.1にも対応するため、この `.ps1` はUTF-8 BOM付きで保存しています。編集時にBOMを取り除かないでください。実行中はNode.jsの日本語出力をUTF-8で扱い、終了後は元の出力設定へ戻します。案内文にスクリプト本体が混じるなどの異常があれば、キーを入力せずCtrl+Cで中断してください。

この操作は公式原文をTypeSafe APIへ送り、利用量に応じて課金されます。キーは実行中の環境変数 `TYPESAFE_API_KEY` にのみ置き、終了時に元へ戻します。環境変数を先に設定済みの場合は非表示入力を省略します。ユーザー・システムの永続環境変数には書き込みません。PowerShellの実行制限がある環境では、制限を勝手に変更せず、その環境の管理方法に従って実行してください。

送信先は `https://api.typesafe.ai/v1/systemone` に固定し、リダイレクトを禁止しています。キーは認証ヘッダーだけに入れ、依頼本文・エラー本文・HTTPヘッダーはログへ出しません。保存するのは許可したモデル名、判定値、使用トークン数、入力原文・出典、日時、入力と依頼のハッシュだけです。

失敗時は既存の候補JSONを変更しません。429・529は短い待機後に最大1回再試行し、長い待機指示がある場合は停止します。途中までAPI判定してから失敗すると、利用量は発生してもその実行分は保存されません。異常終了で `.lock` が残った場合は、分類プロセスが動いていないことを確認してから対象のロックだけを除去してください。

## 人が確認すること

生成後に次を実行し、ローカルサーバーを再起動して `/review` の「分野分類の確認」へ進みます。

```powershell
npm run test:data
npm run typecheck
npm run build
```

まず3件の分野と入力原文を比較し、番号・名称、提案説明の対応、含まれる施策が分類と合うかを確認します。保留や複数分野・複数議案の説明を優先して確認します。3件の確認だけで全52件の精度を証明したり、未確認案件を `verified` にしたりしません。

全件を生成する場合、次の非表示入力を使います。有効な保存済み候補は再利用します。現在は52件すべて保存済みなので、追加の実行は不要です。生成は人の確認ではありません。

```powershell
.\scripts\run-topic-pilot.ps1 -All
```

キーを設定した同じ実行環境では、従来のCLIによる全件・選択生成も利用できます。

```powershell
node --experimental-strip-types scripts/classify-himeji-topics.mjs --run --all
node --experimental-strip-types scripts/classify-himeji-topics.mjs --run --items=bill-135,bill-154 --refresh
```

有効な既存候補は再利用します。`--refresh` は指定した案件を再判定して置き換えます。原文や出典、分類定義・質問が変わった古い候補は、画面表示前の検証で拒否します。ビルドも通常のページ閲覧もAPIへ接続しません。

## 画面から人の確認を記録する

1. `/review#topic-review-heading` で照合する議案を選びます。分類に使った原文と公式リンクが同じ欄に表示されます。
2. 公式資料との対応を見比べ、「候補の分野を確認」「人が分野を指定・修正」「判断を保留」のいずれかを自分で選びます。分野だけの判断で、議員別賛否や議案原文の確認状態は変わりません。
3. 確認者名・判断理由・照合した旨を入力し、この1件のJSONをダウンロードします。確認者名と理由は反映後にサイトへ表示されるため、活動名も使用できます。ダウンロードだけでは反映されません。議案を切り替えると未保存入力は消えます。
4. ファイルパスを実際の保存先に置き換え、事前検証・追記・ビルドを行います。

```powershell
node --experimental-strip-types scripts/import-topic-reviews.mjs "C:\保存先\topic-review-bill-140.json"
node --experimental-strip-types scripts/import-topic-reviews.mjs "C:\保存先\topic-review-bill-140.json" --apply
npm run test:data
npm run typecheck
npm run build
```

ローカルサーバーを再起動すると、確認者・日時・判断理由と分野が反映されます。元のAI応答は変更しません。修正時は以前の記録を削除せず、新しい判断を追記します。同じ記録の二重取り込みや、古い画面からの競合更新は拒否します。最新の画面を読み直して再記録してください。

記録は原文・取得日時・出典・質問の版・AI応答全体のSHA-256と結び付けます。候補を再生成した場合、古い確認は履歴に残して適用対象から外し「再確認待ち」にします。人が判断した証拠は入力した申告であり、本人認証や電子署名ではありません。無記名・理由なし・照合宣言なし・架空の分野・不正な日時は取り込めません。現時点の本番台帳は空で、人の分類確認は0件です。

## 実装の確認結果（2026年9月23日）

- `npm run test:data`：49件成功。重複・古い候補・不正参照・宣言不足・修正履歴の競合・保存失敗時の既存台帳保持を含みます。
- `npm run typecheck`、`npm run build`：成功、59ページを生成。
- Windows PowerShell 5.1：UTF-8 BOM、非表示入力、正常・異常時のキーと文字コード復元、`-All` の引数を実行検証。
- Chrome：375px・1280pxでトップ、議案一覧、第135号・第140号の詳細、議員・会派一覧、確認画面を表示。確認画面の320px・文字200%、キーボードのフォーカス、確認・修正・保留の入力とJSONダウンロードを検証。テスト用の記録は本番台帳に取り込んでいません。
- 画面検証：ポート3005でサーバー起動後に `node --experimental-strip-types scripts/check-topic-review-ui.mjs`。ローカルChromeが必要です。結果と画像は `reports/topic-review-ui/` に保存します。

## 残る確認

実APIの52件の応答は保存・検証済みで、応答に記載された入力トークン数の合計は223,220、出力は9,828です。請求額は確認していません。保留31件のうち28件は判断材料の判定値が0.8未満で、第149・150・151号の3件は個別分野の判定値がすべて0.8未満です。保留はAPI処理の失敗を意味しません。閾値は変更していません。日本語での分類精度と0.8という試行基準の妥当性は未評価です。通信を差し替えた自動テストは、Jevの精度評価ではありません。スマートフォン実機、スクリーンリーダー、全画面での拡大操作は未確認です。
