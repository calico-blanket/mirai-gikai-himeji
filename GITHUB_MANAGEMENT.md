# GitHub管理

- 管理先： https://github.com/calico-blanket/mirai-gikai-himeji
- 公開範囲：プログラム、テスト、独自の画像・アイコン、開発説明書。
- 非公開のまま手元に残すもの：`data/` の実データ、`reports/` の照合記録・画像、`.env*`、開発用Skill、ビルド結果と作業用ファイル。
- Webサイトのデプロイ、GitHub Pagesの有効化は行わない。

データは `.gitignore` で除外し、`data/README.md` だけを登録します。GitHubにあるコードだけでは起動・ビルドできないことをREADMEにも明記しています。データは別途バックアップが必要です。人による確認記録を、再取得やAI生成で復元したことにはしません。

Gitの登録名は既存設定を使い、メールアドレスはGitHubのnoreplyアドレスを使います。APIキーはコミット・コマンド引数・ログに記録しません。

今回の初回登録後も、変更は小さく検証してから記録します。原資料を強制追加したり、別プロジェクトのGit設定を変更したりしないでください。コード・素材の再利用ライセンスは未指定です。原資料には各提供元の条件があり、コードと一括して利用許諾しません。
