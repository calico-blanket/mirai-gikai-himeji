# 姫路の議会を知る：残る実装の設計

対象は令和7年第4回定例会です。公式資料の原文、機械抽出候補、このサービスの整理、目視確認記録を別々に保存・表示します。現在のデータは公式HTMLの52案件、PDFの45人・11会派・2,340票候補です。過去の目視確認を特定できるのは議案第135号の公式HTMLの4項目だけです。

## 1. 確認記録と差分対応（今回実装）

- 原PDFを固定したうえで、見出しの列、票のページ・行・列を指定して確認する。作業計画を5組で生成でき、必要に応じて範囲を小さくする。
- 一致した範囲だけ `verified` 記録とし、相違は内容を添えて `needs_correction` とする。機械抽出JSONは元の候補として保存する。PDFまたは候補値が変われば古い記録を拒否する。
- 議案第135号のHTML確認は票へ流用しない。見出しの確認も、採決日ごとの会派所属の証明には使わない。
- 人の作業を減らす次の改善は、独立した再抽出・差分表と、相違・特殊記号・固定シードの標本を優先する画面。未照合の範囲は標本の結果から `verified` にしない。

## 2. 議案本文と会議録の対応付け

着手状況：市長提出51件の本文開始ページ、全52件の市議会側詳細リンク、議案番号を明記した公式質問項目2件を機械照合して画面に追加しました。これらは人による確認前です。質問項目は発言本文・答弁・会議録との対応を証明しません。

追加実装：本会議5日分・661発言を保存し、51案件に72件の対応候補（うち提案説明51件）を掲載しました。出典の版、原文抜粋、発言ID、文脈による答弁対応の根拠を検証します。すべて人による確認前です。確認ページに例外を優先表示し、人が原文を探す手間を減らしています。委員会内の会議録、番号を述べない継続質疑、議員提出議案第7号への対応は残作業です。会議録用の確認台帳は、候補ID・原資料ハッシュ・抜粋と根拠のハッシュを固定し、原文一致と議案対応の判断を別項目として記録する仕組みを実装済みです。PDF票用台帳からの承認は引き継ぎません。

- 公式の議案本文PDFと会議録から、議案番号・会議日・発言箇所・ページを候補として抽出する。会議録の検索結果だけで議論の内容を確定しない。
- 番号だけで誤結合しないよう、会期・正式名称・審議日・提出資料の種類を照合する。議案ごとに「対応あり」「候補のみ」「見つからない」「資料未取得」を分ける。
- 画面では会議録の原文引用を短く示し、発言者と日時、公式資料への直接リンクを添える。長い会議録を切り貼りした文章を公式要約として扱わない。
- 確認方法は、議案番号の重複・曖昧な一致・リンク先のページ違いを検出するテストと、人が差異・例外・標本を確認できる一覧。

## 3. 平易な説明と分野分類

分野分類の実装：公式HTTP API、9分野の複数該当、根拠不足による保留、入力原文と質問の版管理、既存候補の再利用を追加しました。分類は専用CLIだけで実行し、画面やビルドからAPIを呼びません。2026年9月23日にユーザーが実APIで全52案件を生成し、保存結果を検証しました。21件が分野候補（複数分野9件）、31件が分類保留、未生成0件で、すべて人による確認前です。詳細は `TYPESAFE_TOPICS.md`。

分類の人による確認台帳と原文照合フォームを追加しました。候補を確認・修正・保留した個別の判断を原文・候補全体のハッシュと結び付け、画面からダウンロード、CLIで検証・追記します。変更履歴は残し、候補更新後の古い確認は適用しません。原資料照合の記録はまだ0件です。4件の試用フィードバックは別の開発記録で保持しています。Windows PowerShell 5.1用の `-All` で未生成案件を分類できます。

2026年9月23日、全52件・61項目の短い説明案を追加しました。原文・出典・取得日時のスナップショットに各文を結び付け、原文更新や編集漏れをビルド時に検出します。詳細で根拠を開け、一覧で説明文を検索でき、確認ページには17件の注意対象と全件への入口があります。すべてAI作成・人による確認前です。55件のテスト、型チェック、59ページのビルド、Chromeのスマホ・PC幅9ページと説明・根拠のキーボード操作を検証しました。詳細は `EXPLANATIONS.md`。説明と会議録対応の人による承認・訂正台帳は追加済みです。日本語の分類精度評価と、議案番号のない発言への関連判定は残作業です。

- 説明は「このサービスによる説明」と明記し、原資料の節・ページへの根拠を保持する。原資料で裏付けられない主張、政治的な評価、賛否の推奨を掲載しない。
- 分野分類は固定した分類語彙と根拠箇所を持つ候補として生成し、人が確認するまで確定表示しない。TypeSafe Jev APIの利用は、この候補生成に有効か小規模に評価してから決める。APIキーは環境変数だけに置き、コード・JSON・ログには記録しない。
- 確認方法は原資料との対応、誤分類、説明の誤読を含む標本確認。AI出力そのものを公式情報とみなさない。

## 4. 公開前の確認

2026年9月24日更新：説明・会議録の確認と訂正の台帳、複数判断の保存、ファイル選択による反映を実装済みです。説明は大人向け・小学生向けの各52件を別に保存し、相互に承認を流用しません。AIチャットは要件から除外しました。読者モードと更新時の再利用、操作方法は `READER_MODES.md` / `USER_GUIDE.md` を参照してください。

- 対象会期と取得日、非公式であること、未確認範囲を全ページで明示する。未確認の票や説明を確定した事実として見せない。
- スマートフォン・PC・文字拡大・キーボードで実画面を確認する。型・データ整合性・リンク・ビルドを確認し、確認記録のない項目を `verified` にしないことをテストする。
- 公開判断は、公式資料の版、確認範囲と担当者、未解決の相違、表示上の注意書きが揃ってから行う。
