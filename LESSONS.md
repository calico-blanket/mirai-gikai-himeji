## 2026-09-22 スマートフォン幅の画面確認

- 症状: Chromeの `--window-size=375,900` による最初の画像は右側が切れて見えた。
- 根本原因: 画像の幅指定だけではCSSの表示領域が375pxになっていなかった。CDPで幅を明示した再確認では `innerWidth=375`、`scrollWidth=375` だった。
- 教訓: モバイル表示の確認では画像のピクセル幅だけを根拠にせず、ブラウザの表示領域と横スクロール幅を実測する。

## 2026-09-22 Windows上のPDF文字抽出

- 症状: PowerShell経由のPython出力で議員名が文字化けして見え、PDF側の文字コード不備と誤認しそうになった。
- 根本原因: Pythonの標準出力がCP932で、実行ツール側の表示と食い違った。PDF抽出自体は日本語を保持していた。
- 教訓: 抽出精度を判断する前に `sys.stdout.reconfigure(encoding="utf-8")` で表示経路を確認する。PDFの列は位置と全ページの見出し一致も検査する。

## 2026-09-22 Next.js用データ検証の単体テスト

- 症状: Node.jsの型除去機能で、Next.js用モジュールを直接読み込むテストが拡張子なしの相対importを解決できず失敗した。
- 根本原因: Node.jsの実行経路ではNext.jsのモジュール解決が適用されない。エラーは `ERR_MODULE_NOT_FOUND` で `lib/data/council` を指した。
- 教訓: JSON読込・画面接続とは分けて、純粋な検証関数をNode.jsから直接テストできるモジュールに置く。

## 2026-09-23 Windows PowerShell 5.1の日本語スクリプト

- 症状: APIキー入力の案内が文字化けし、後続のコードまで入力プロンプトに表示された。
- 根本原因: UTF-8 BOMなしの `.ps1` をWindows PowerShell 5.1（既定CP932）が誤読した。ParseFileの構文エラーは0件でも、Read-Host内のAsSecureStringパラメーターが0件となり、命令の長さが446文字に伸びた。PowerShell 7での構文確認だけでは検出できなかった。
- 教訓: 日本語を含むWindows用 `.ps1` はUTF-8 BOM付きにし、5.1の実行系で入力プロンプトと非表示指定まで検証する。Node経由で5.1をテストするときは、継承されたPowerShell 7のPSModulePathを除去する。実測で7のSecurityモジュールを誤って選び、ConvertTo-SecureStringの自動読込が失敗していた。実キー・実APIは使わず入力と通信を差し替え、正常・異常時の環境変数復元を確認する。

## 2026-09-23 複数ファイルのパッチとブラウザのキー操作

- 症状: 説明案用の親ディレクトリ作成でパッチが失敗したが、先に指定した検証モジュールは作成済みだった。
- 根本原因: 複数ファイルのパッチは途中まで適用されていた。失敗後の `Test-Path` で先頭ファイルがTrue、後続スクリプトがFalseであることを確認した。
- 教訓: パッチ失敗時は全体が取り消されたと仮定せず、適用済み範囲を確認してから再開する。
- 別の観測: CDPのEnter送信でkey/codeだけではdetailsが開かなかった。同じ画面でtext/unmodifiedTextに改行を指定した再実行では開閉に成功した。
- 教訓: UIの欠陥と判定する前に、フォーカスとブラウザへの入力イベントが実際の操作に対応しているか確かめる。
